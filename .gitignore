node_modules/
.env
lumi-harness/.env
admin-panel/.env
n8n/.env
auth/
screenshots/
logs/

# backfill: dados de pacientes (PII) ficam local, so o script vai pro repo
db/backfill/*.json

# backup: dumps tem dado real de paciente (PII), nunca vao pro repo. O
# config real tambem fica de fora (nao tem senha, mas nao precisa ser
# publico) -- so o .example.json e commitado.
backups/
scripts/backup-databases.json

# deploy-hook-*.sh: URL do webhook de deploy do Easypanel tem o token
# embutido (mesmo padrao do painel-financeiro, outro projeto)
deploy-hook-*.sh

# Asaas API key (checkout) mora aqui se/quando o site ganhar backend
site/.env
